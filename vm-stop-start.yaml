---
# Stop and/or start a Proxmox VM
# File: vm-stop-start.yaml

- hosts: localhost
  pre_tasks:
    - name: 'Fail if required variables are not defined'
      ansible.builtin.fail:
        msg: 'The variable ''{{ required_var }}'' is required.'
      when: required_var not in hostvars[inventory_hostname]
      loop:
        - vm_hostname
      loop_control:
        loop_var: required_var

    - name: 'Print supported action variable values if one is not supplied'
      ansible.builtin.debug:
        msg: 'The variable action must be one of the following values: status, started, stopped, restarted, paused, hibernated.'
      when: action not in [ 'status', 'started', 'stopped', 'restarted', 'paused', 'hibernated' ]

  roles:
    - role: proxmox-qemu-stop-start
